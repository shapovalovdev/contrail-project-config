---

- hosts: localhost
  roles:
    - role: skip-job
      vars:
        job_name: 'contrail-vnc-build-unittest-ubuntu-trusty'

- hosts: all
    - contrail-common-libs
    - contrail-controller-hacks
    - packaging-prepare-contrail
    - contrail-unittests

